{
  "name": "alexis",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "npm run clean && npm run build:html && npm run build:css && npm run build:assets && npm run build:scripts",
    "build:css": "node scripts/build-css.js",
    "build:html": "node scripts/build-html.js",
    "build:assets": "node scripts/build-assets.js",
    "build:scripts": "node scripts/build-scripts.js",
    "clean": "node scripts/clean.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bootstrap": "^4.6.0",
    "jquery": "^3.5.1",
    "jquery.easing": "^1.4.1"
  },
  "devDependencies": {
    "autoprefixer": "^10.2.4",
    "cssnano": "^4.1.10",
    "postcss": "^8.2.4",
    "sass": "^1.32.6",
    "shelljs": "^0.8.4",
    "upath": "^2.0.1"
  }
}
